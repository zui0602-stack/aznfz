# floatDim2int/parseInt/parseFloat/parseBoolean

- 分類：04_全局函數
- 類型：JavaScript 函數庫
- 官方來源入口：https://www.aznfz.com/document/prepare
- 建議用途：給 AI 寫冰狐智能輔助腳本時查閱

## 這頁是什麼

`floatDim2int/parseInt/parseFloat/parseBoolean` 屬於「04_全局函數」分類。AI 看到這個條目時，應把它當成 `JavaScript 函數庫` 相關能力來理解。

## 什麼時候要讀這頁

- 寫腳本資料處理時使用，例如陣列、字串、日期、JSON、正則、數學與控制台。
- 把角色列表、資源種類、倒數時間、OCR 結果轉成可判斷資料時會用到。

## 腳本設計規則

- 資料處理要先轉型、判空、檢查長度，再操作。
- 日期時間要使用 24 小時制，跨日區間要獨立處理。
- 陣列循環處理角色時，要避免索引越界。

## 容易踩坑

- 不要把 OCR 結果直接當可靠資料；需 trim、過濾、容錯。
- 不要把時間字串直接比較大小；要先轉成分鐘或時間戳。

## 萬龍覺醒可用場景

- 角色清單去重、資源策略輪換、採集倒數排序、日誌 JSON 化。

## AI 實作時要問自己的問題

1. 這個功能是不是目前流程必要？
2. 有沒有更穩、更簡單的模組可以替代？
3. 需要哪些權限？
4. 是否需要截圖/OCR/無障礙/ADB/HID/Root？
5. 成功條件是什麼？
6. 失敗時要重試、切換策略、還是退出？
7. 是否要寫入日誌或保存狀態？

## 建議輸出格式

```text
功能：floatDim2int/parseInt/parseFloat/parseBoolean
分類：04_全局函數
使用原因：
前置條件：
核心步驟：
成功判斷：
失敗處理：
測試方法：
```

## 來源備註

這份筆記是從官方文檔目錄整理出的 AI 使用筆記，不是官方原文逐字複製。需要精確函數簽名、參數與官方範例時，回官方文檔搜尋「floatDim2int/parseInt/parseFloat/parseBoolean」。
